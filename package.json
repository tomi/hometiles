{
  "name": "starttiles",
  "version": "0.0.0",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build"
  },
  "devDependencies": {
    "turbo": "1.6.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
